
const initialState = {
  users: [],
  isFetching: false,
  hasError: false
};

function githubReducer (state=initialState, action){
  switch (action.type) {
    default:
      return state
  }
}

export default githubReducer;